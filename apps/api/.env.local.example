# Copy values into apps/api/.env for local PostgreSQL (no Docker)
# Use the same credentials as your DataGrip connection (root @ localhost:5432)

PGUSER=root
PGPASSWORD=your_datagrip_password_here
DATABASE_URL=postgresql://root:your_datagrip_password_here@127.0.0.1:5432/cvscreening
